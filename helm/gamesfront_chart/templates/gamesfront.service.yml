apiVersion: v1
kind: Service
metadata:
  name: gamesfront
spec:
  selector:
      app: gamesfront
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8000
#      nodePort: 31134
#  type: NodePort

